export = {
        url: "{{ enterprise_config.diploma_issuer.url }}",
        port: "{{ enterprise_config.diploma_issuer.port }}",
        appSecret: "{{ enterprise_config.diploma_issuer.secret }}",
        db: {
                host: "wallet-db",
                port: "3307",
                username: "root",
                password: "root",
                dbname: "issuer"
        },
        wwwalletURL: "{{ wallet_frontend.url }}/cb"
}
